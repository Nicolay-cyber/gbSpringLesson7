server:
  servlet:
    context-path: /app
  port: 8189
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:C:/Users/Николай/Desktop/gbSpringLesson7/Database.db
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLiteDialect

